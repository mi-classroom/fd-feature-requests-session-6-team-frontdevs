document.addEventListener('DOMContentLoaded', (event) {

    let buttons = document.querySelectorAll('[data-button-js]')

    buttons.addEventListener('click', (event) => {
        //TODO: Logik von Copilot -> es fehlt die Logik in HTML/ CSS für active
       
    })

})